{
  "name": "inventory-e2e-tests",
  "version": "1.0.0",
  "description": "Playwright end-to-end tests for the Warehouse Inventory app (React frontend + Spring Boot backend)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
